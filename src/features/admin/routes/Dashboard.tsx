import Count from "../components/Count";

type DashboardProps = {};

const Dashboard = (props: DashboardProps) => {
  return (
    <>
      <h4>Dashboard</h4>
      <Count />
    </>
  );
};

export default Dashboard;